<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= locals.user.name %>'s Profile</title>
    <%- include('./partials/head.ejs') %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: url('/backgrounds/133460286_10262642.jpg');
                background-size: cover;
                background-attachment: fixed;
                background-position: center center;
                background-repeat: no-repeat;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .profile-container {
            max-width: 600px;
            margin:20px auto;
            background-color:white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .profile-container img {
            width: 20vh;
            height: 20vh;
            border-radius: 50%;
            border: 4px solid #d2a197;
            margin-bottom: 20px;
        }

        .profile-container h2 {
            color: #ae7763;
            font-size: 2.5em;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }

        .profile-details label {
            font-size: 1.1em;
            font-weight: 600;
            color: #a37971;
        }

        .edit-button {
            margin-top: 20px;
            font-size: 1.2em;
            color: white;
            background-color: #d2a197;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
        }

        .edit-button:hover {
            background-color: #c88e7d;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="container">
        <div class="profile-container">
            <img src="<%= user.profileImageURL %>" alt="Profile Picture">
            <h2><%= user.name %></h2>
            <div class="profile-details mt-4 text-start">
                <div class="mb-3">
                    <label>Name:</label>
                    <p class="form-control bg-light border-0"><%= user.name %></p>
                </div>
                <div class="mb-3">
                    <label>Email:</label>
                    <p class="form-control bg-light border-0"><%= user.email %></p>
                </div>
                <div class="mb-3">
                    <label>About:</label>
                    <p class="form-control bg-light border-0"><%= user.about || "No details provided" %></p>
                </div>
                <div class="mb-3">
                    <label>Role:</label>
                    <p class="form-control bg-light border-0"><%= user.role %></p>
                </div>
            </div>
            <a href="/user/profile/edit" class="btn edit-button">Edit Profile</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <%- include('./partials/scripts.ejs') %>
</body>
</html>
