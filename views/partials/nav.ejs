<nav class="navbar navbar-expand-lg bg-dark bg-opacity-75" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand text-light" href="#">ScribeFables</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"> <!-- Align the nav items to the left -->
        <li class="nav-item">
          <a class="nav-link " aria-current="page" href="/">Home</a>
        </li>
        <% if (locals.user) { %>
        <li class="nav-item">
          <a class="nav-link" href="/blog/add-new">Add Blog</a>
        </li>
        <li class="nav-item"></li>
        <a class="nav-link" href="/blog/my-blogs">My Blogs</a>
        </li>
        <% } %>
      </ul>
      <!-- User info aligned to the right -->
      <% if (locals.user) { %>
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle"
            href="#"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <%= locals.user.name %>
            <img src="<%= locals.user.profileImageURL %>" class="profile-image" alt="<%= locals.user.name %> Profile Image" />
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
            <li><a class="dropdown-item" href="/user/profile">Profile</a></li>
          </ul>
        </li>
      </ul>
      <% } else {%>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/user/signup">Create Account</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/user/signin">Signin</a>
        </li>
      </ul>
      <% } %>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
<div class="container mt-4">
  <div class="alert alert-danger" role="alert"><%= locals.error %></div>
</div>
<% } %>

<style>
  /* Navbar background and branding */
  .navbar {
    background-color: #caada7 !important;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .navbar-brand {
    font-size: 1.8em;
    font-weight: bold;
  }

  /* Navbar items */
  .nav-link {
    color: #ffffff !important;
    font-size: 1.1em;
    transition: color 0.3s ease;
  }

  .nav-link:hover {
    color: #8d665e !important;
  }

  /* Active nav-link style */
  .nav-link.active {
    background-color: #d2a197;
    color: white !important;
    border-radius: 5px;
  }

  .nav-item.dropdown .nav-link {
    color: #ffffff !important;
  }

  /* Dropdown menu styling */
  .dropdown-menu {
    background-color: #fce4ec;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    padding: 4px 2px;
    position: absolute;
    right: 0; /* Align the dropdown to the right edge */
    margin-right: 30px; /* Space from the right edge */
    transform: translateX(-20%); /* Move the dropdown slightly to the left */
  }

  .dropdown-item {
    color: #8d665e;
    text-align: center;
  }

  .dropdown-item:hover {
    background-color: #d2a197;
    color: white;
  }

  /* Dropdown profile image size and spacing */
  .nav-item.dropdown .profile-image {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    object-fit: cover;
    margin-right: 10px;
  }

  /* Toggle button */
  .navbar-toggler-icon {
    background-color: #8d665e;
  }

  /* Ensure relative positioning for dropdown alignment */
  .navbar-nav {
    position: relative;
  }
</style>
